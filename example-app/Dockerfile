# Use PostgreSQL 18 as the base image
# (Note: In May 2026, this should be the current or latest stable version)
FROM postgres:18

# Set default environment variables for the database
ENV POSTGRES_DB=konze_db
ENV POSTGRES_USER=konze_user
ENV POSTGRES_PASSWORD=konze_password

# Expose the default PostgreSQL port
EXPOSE 5432
